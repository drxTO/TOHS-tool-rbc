{
  "name": "tohs-rbc-pipeline",
  "version": "0.1.0",
  "description": "Rental Background Check (RBC) daily data pipeline: fetches Toronto Open Data and Ontario LTB public records, normalizes addresses, and commits address-keyed JSON shards.",
  "private": true,
  "type": "commonjs",
  "license": "MIT",
  "scripts": {
    "sync": "node src/sync.js",
    "sync:dry": "node src/sync.js --dry-run",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "proj4": "^2.21.0"
  }
}
